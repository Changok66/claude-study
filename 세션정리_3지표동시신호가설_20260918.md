# 세션정리 — 엔상하/선3일목/피보일목 3지표 동시신호 가설 검증 (2026-09-18)

## 배경

기존에 검증했던 엔상5/엔하5 반전각도 분석(`src/reversal_angle.py`,
`run_reversal_angle_analysis.py`)은 밴드 하나만 봤을 때의 신호였다. 이번엔
"엔상하(밴드터치) + 선3일목(추세전환) + 피보일목(크로스+구름돌파)" 세 지표를
동시에 켜놓고, 세 지표가 같은 시점 근처에서 함께 신호를 낼 때 매매 효과
(승률/손익비)가 실제로 극대화되는지 검증해달라는 요청에서 시작했다.

가설:
1. 엔상하: 엔하4/5 밴드 터치
2. 선3일목: 50일선(엔상1)+선2+주가가 다같이 상승 전환
3. 피보일목: 피보4가 피보5를 상향 교차 + 구름 돌파

검증 방법(요청받은 4단계 그대로): 1) 각 지표 신호 발생 시점 목록화 →
2) 신호 간 시간 거리 분포 확인 → 3) 1개/2개/3개 동시 신호 그룹별 승률/손익비
비교 → 4) 동시성이 높을수록 단조적으로 좋아지는지 확인. 큰 작업이라 먼저
Plan 모드로 설계를 확정하고(계획 파일: `noble-juggling-feigenbaum.md`) 진행했다.

## 진행 결과

### 1. 기존 코드 재사용 — 새 지표 계산 없이 신호 판정 계층만 추가

세 지표의 계산 공식(엔상/엔하 50,20,1.5 / 선3일목 10,20,50 / 피보일목
5,8,13)은 이미 `src/reversal_angle.py`에 구현돼 있어서 그대로 재사용했고,
그 위에 새 신호 정의만 추가했다:

- `src/triple_indicator_confluence.py` (신규): 신호1(엔상하)/신호2(선3일목)/
  신호3(피보일목) 판정, 신호 간 거리 계산(`_nearest_distance`, numpy
  searchsorted 기반), 동시신호개수(1~3) 부여
- `run_triple_indicator_confluence.py` (신규): 9종목 순회 → 4단계 검증 절차
  실행 → `data/triple_indicator_confluence_backtest.csv` 저장

가설 문구 중 계산식이 명시되지 않았던 부분은 이 프로젝트의 기존 관례에 맞춰
아래처럼 확정했다(코드 주석에도 근거를 남김):
- "상승 전환" = 오늘값 > 어제값 이면서 어제값 <= 그제값 (하락/보합에서
  상승으로 막 바뀐 전환점), 엔상1/선2/종가 세 값 모두 같은 날 성립해야 함
- "구름 돌파" = 종가가 구름 상단(max(선1,선2))을 오늘 처음 넘어선 날
- 매매 시뮬레이션은 기존 `simulate_reversal_trades`(엔하N 터치→롱 진입, MA50
  복귀 또는 5봉 경과 시 청산)를 그대로 재사용해서 새 매매 규칙을 만들지 않음
- 동시성 판정 윈도우는 값을 미리 정하지 않고, 2단계에서 실제 관측된 신호 간
  거리 분포의 중앙값으로 스크립트 실행 시점에 자동 결정하도록 구현

### 2. 실행 결과 — 3중 동시신호 표본이 0건으로 가설 검증 불가

9종목(삼성전자, SK하이닉스, 두산에너빌리티, 테크윙, 아모레퍼시픽,
HD현대중공업, HD현대일렉트릭, 에이피알, 대한광통신) 전체에서 신호1(엔상하)
이벤트 499건을 기준으로:

| 단계 | 결과 |
|---|---|
| 2단계: 신호 간 거리 | 신호2(선3일목) 발견율 **0.6%**(499건 중 3건), 신호3(피보일목) 발견율 **2.8%**(14건) — 신호1과 근접해서 뜨는 경우 자체가 매우 드묾 |
| 자동 결정된 윈도우 | 분포 중앙값 기준 ±8거래일 |
| 3단계: 그룹별 성과 | 동시신호 1개: 학습 330건/검증 157건(승률 52.7%/59.2%, 손익비 1.22/1.22) — 표본 충분. 동시신호 2개: 학습 6건/검증 6건 — n<30 참고용. **동시신호 3개: 0건** |
| 4단계: 단조성 | 학습구간은 승률↑(52.7%→83.3%)이지만 손익비↓(1.22→0.99), 검증구간은 반대로 손익비↑(1.22→4.10)이지만 승률↓(59.2%→33.3%) — 방향이 서로 엇갈리고 n=6이라 우연일 가능성이 큼 |

건수 검산: 전체 거래 499건 = 그룹별 합계 499건(1개 487건 + 2개 12건) 일치 확인.

### 3. 결론 — 가설 기각(표본 부족)

세 지표를 위 정의로 계산했을 때, **3개 신호가 동시에(±8거래일 이내) 일치한
사례가 9종목 전체 역사 데이터에서 단 한 번도 없었다.** "동시성이 높을수록
매매 효과가 극대화된다"는 가설을 검증할 표본 자체가 존재하지 않고, 2개
동시신호 그룹(n=6)도 학습/검증 결과가 서로 모순돼 신뢰할 수 없는 수준이다.
→ **가설 기각.**

원인 추정: 세 지표가 서로 다른 성격의 조건(밴드 터치=변동성 기반,
상승전환=추세 반전, 골든크로스+구름돌파=모멘텀 확인)이라 설계상 애초에
같은 날 겹치기 어렵고, 특히 신호2(3계열 동시 상승전환)는 조건이 세 겹으로
엄격해서(엔상1/선2/종가가 "같은 날" 전환) 발생 자체가 극히 희소했다.

## 커밋 이력

- `25a8f00` 3지표 동시신호 가설 검증 코드 + 결과 CSV(499건) 커밋

## 다음 단계 후보

- 신호2("다같이 상승 전환")의 "같은 날 동시" 요건을 완화(예: ±1~2일 이내
  각자 전환하면 인정)하면 발생빈도가 얼마나 늘어나는지 확인해볼 수 있음 —
  다만 조건을 완화할수록 원래 가설("세 지표가 진짜 동시에 일치")과는
  멀어지므로, 완화가 타당한지는 별도 판단 필요
- 동시신호 2개 그룹(n=6)은 표본이 너무 적어 결론을 못 내렸는데, 관찰기간을
  더 늘리거나(다른 종목 추가) 밴드 번호(엔하2/3)까지 넓혀서 표본을 키우면
  재검증 가능할 수 있음
- 이번에도(반전각도 분석에 이어) 기계적 필터/조합 탐색이 표본 부족 또는
  기각으로 끝나는 패턴이 반복됨 — 재량적 판단(discretionary) 영역에서
  후보를 찾는 쪽이 더 생산적일 수 있음
